{
  "name": "graphinx",
  "version": "0.0.1",
  "workspaces": ["packages/*"],
  "scripts": {
    "check": "biome check --apply .",
    "build-config-types": "yq . < config.schema.yaml > config.schema.json && quicktype --src-lang schema config.schema.json -o packages/graphinx/src/config.ts",
    "dev": "nodemon --watch config.schema.yaml --exec 'bun run build-config-types'"
  },
  "devDependencies": {
    "@biomejs/biome": "1.7.0",
    "nodemon": "^3.1.4",
    "quicktype": "^23.0.170",
    "typedoc": "^0.25.13"
  }
}
